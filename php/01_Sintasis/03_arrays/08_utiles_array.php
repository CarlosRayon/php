<?php

//continue          Util en los bucles. Salta la interracion del bucle actual hasta la siguiente 
//break             Util en los bucles. Rompe la interacion del bucle y sale de el

//var_dump(<array>); Muestra los datos del array
//print_r(<array>);  Muestra los datos del array

//current(array):    Me da el valor de la posicon actual del array  En conjunto se pueden usar con un while
  
//key(array);       Me da la clave de la posicion actual del array

//next(array);       Avanza una posicon el array

//count()           Contar los elementos que tiene el array

//unset($paises["spain"]); Destruye una variable especificada(destruye el elemento, la clave y el valor)

//array_key_exists ( $key ,$array );Verifica si el índice o clave dada existe en el array

// print_r(array_values($paises));Devuelve los valores del array   Las podemos guardar en otro array y comparar

// print_r(array_keys($usuarios[0]));Devuelve las keys de un array concreto Las podemos guardar en otro array y comparar

//array_push ( $array , $value1 [, mixed $... ] )  Inserta uno o más elementos al final de un array

//in_array(<datoBuscar>, $arrayDondebuscar)  Comprueba si el dato esta en un array. Retorna true si esta o false si no lo esta